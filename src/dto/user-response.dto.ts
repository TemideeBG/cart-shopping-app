/*import { User } from "../entity/User";

export class UserResponse {
  first_name: string;
  last_name: string;
  full_name: string;
  email: string;
  role: string;
  nationality: string;
  home_address: string;
  city: string;
  gender: string;
  date_of_birth: Date;
  age: number;
  phone_number: string;

  // Static method to create UserResponse instance from a User entity
  static fromUser(user: User): UserResponse {
    const userResponse = new UserResponse();
    userResponse.first_name = user.first_name;
    userResponse.last_name = user.last_name;
    userResponse.full_name = user.full_name;
    userResponse.email = user.email;
    userResponse.role = user.role;
    userResponse.nationality = user.nationality;
    userResponse.home_address = user.home_address;
    userResponse.city = user.city;
    userResponse.gender = user.gender;
    userResponse.date_of_birth = user.date_of_birth;
    userResponse.age = user.age;
    userResponse.phone_number = user.phone_number;

    return userResponse;
  }
}

*/